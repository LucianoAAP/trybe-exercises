const ping = require('./ping');
const { uploadOne, uploadMultiple, uploadProfile, getProfile } = require('./upload');

module.exports = {
  ping,
  uploadOne,
  uploadMultiple,
  uploadProfile,
  getProfile,
};
